# Copy to .env.production and fill in real values. Gitignored.

POSTGRES_PASSWORD=change-me-prod

# Empty for local registry (no auth). Set to a GitHub PAT (write:packages)
# when migrating registry.server to ghcr.io.
KAMAL_REGISTRY_PASSWORD=
